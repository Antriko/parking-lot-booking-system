{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --hostname '0' --port 3050",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.1"
  },
  "dependencies": {
    "@nuxt/vite-builder": "^3.0.0-rc.3"
  }
}
